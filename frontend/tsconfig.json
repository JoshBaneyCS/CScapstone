{
  // =============================================================================
  // TYPESCRIPT CONFIGURATION FOR CASINO CAPSTONE FRONTEND
  // =============================================================================
  // This file configures the TypeScript compiler for our React application.
  // TypeScript adds static type checking to JavaScript, catching errors at
  // compile time instead of runtime.
  //
  // Benefits of TypeScript:
  //   - Catch type errors before running the code
  //   - Better IDE support (autocomplete, refactoring)
  //   - Self-documenting code (types show what data looks like)
  //   - Safer refactoring (compiler catches breaking changes)
  //
  // To check types without building:
  //   npm run type-check
  // =============================================================================

  "compilerOptions": {
    // -------------------------------------------------------------------------
    // TARGET AND MODULE SETTINGS
    // -------------------------------------------------------------------------
    
    // target: Which JavaScript version to compile to
    // ES2020 is well-supported by modern browsers and includes features like
    // optional chaining (?.) and nullish coalescing (??)
    "target": "ES2020",
    
    // lib: Which built-in type definitions to include
    // - ES2020: Modern JavaScript APIs
    // - DOM: Browser APIs (document, window, etc.)
    // - DOM.Iterable: Iterable DOM collections (NodeList, etc.)
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    
    // module: What module system to use in output
    // ESNext uses native ES modules (import/export)
    "module": "ESNext",
    
    // moduleResolution: How to resolve import statements
    // "bundler" is optimized for bundlers like Vite that handle resolution
    "moduleResolution": "bundler",
    
    // -------------------------------------------------------------------------
    // JSX SETTINGS (React)
    // -------------------------------------------------------------------------
    
    // jsx: How to handle JSX syntax
    // "react-jsx" uses the new JSX transform (React 17+)
    // This means you don't need to import React in every file
    "jsx": "react-jsx",

    // -------------------------------------------------------------------------
    // STRICT TYPE CHECKING
    // -------------------------------------------------------------------------
    // These options enable strict type checking for safer code
    
    // strict: Enable all strict type checking options
    // This is a shorthand for enabling many individual strict options
    "strict": true,
    
    // noUnusedLocals: Error on unused local variables
    // Helps keep code clean by flagging dead code
    "noUnusedLocals": true,
    
    // noUnusedParameters: Error on unused function parameters
    // Use _ prefix for intentionally unused params (e.g., _event)
    "noUnusedParameters": true,
    
    // noFallthroughCasesInSwitch: Error on switch case fallthrough
    // Prevents accidental missing break statements
    "noFallthroughCasesInSwitch": true,
    
    // noUncheckedIndexedAccess: Array/object access returns T | undefined
    // Forces you to handle the case where an index might not exist
    "noUncheckedIndexedAccess": true,

    // -------------------------------------------------------------------------
    // MODULE INTEROP
    // -------------------------------------------------------------------------
    
    // esModuleInterop: Better compatibility with CommonJS modules
    // Allows default imports from modules without default exports
    "esModuleInterop": true,
    
    // allowSyntheticDefaultImports: Allow default imports from modules
    // without explicit default export (for type checking only)
    "allowSyntheticDefaultImports": true,
    
    // isolatedModules: Ensure each file can be transpiled independently
    // Required for tools like Vite that process files separately
    "isolatedModules": true,
    
    // allowImportingTsExtensions: Allow importing .ts/.tsx files
    // Used with noEmit since bundler handles the actual imports
    "allowImportingTsExtensions": true,

    // -------------------------------------------------------------------------
    // OUTPUT SETTINGS
    // -------------------------------------------------------------------------
    
    // noEmit: Don't output JavaScript files
    // Vite handles compilation; TypeScript is only for type checking
    "noEmit": true,
    
    // skipLibCheck: Skip type checking of declaration files (.d.ts)
    // Speeds up compilation; library types are usually correct
    "skipLibCheck": true,

    // -------------------------------------------------------------------------
    // PATH RESOLUTION
    // -------------------------------------------------------------------------
    
    // baseUrl: Base directory for resolving non-relative imports
    "baseUrl": "capstone-groupe.com",
    
    // paths: Path aliases for cleaner imports
    // Instead of: import { Button } from '../../../components/Button'
    // You can use: import { Button } from '@/components/Button'
    "paths": {
      "@/*": ["src/*"]
    },

    // -------------------------------------------------------------------------
    // TYPE DEFINITION SETTINGS
    // -------------------------------------------------------------------------
    
    // resolveJsonModule: Allow importing JSON files
    // Useful for configuration files or static data
    "resolveJsonModule": true,
    
    // types: Which @types packages to include
    // Empty array means only include types that are explicitly imported
    "types": ["vite/client", "vitest/globals"]
  },

  // ===========================================================================
  // FILE INCLUSION/EXCLUSION
  // ===========================================================================
  
  // include: Which files to type check
  // - src: All source code
  // - vite.config.ts: Vite configuration (if using TypeScript config)
  "include": ["src"],
  
  // exclude: Which files to skip (even if matched by include)
  // node_modules is excluded by default, but we're explicit here
  "exclude": ["node_modules", "dist"],

  // ===========================================================================
  // REFERENCES (for project references if needed)
  // ===========================================================================
  // Reference tsconfig.node.json for vite and vitest config files
  "references": [{ "path": "./tsconfig.node.json" }]
}