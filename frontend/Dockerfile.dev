# =============================================================================
# FILE: frontend/Dockerfile.dev
# =============================================================================
# Development Dockerfile for React/Vite frontend with hot reloading.
#
# This is for LOCAL DEVELOPMENT ONLY. For production, use frontend/Dockerfile.
#
# Features:
#   - Vite dev server with HMR (Hot Module Replacement)
#   - Source files mounted as volume for instant updates
#   - No rebuild needed when code changes
# =============================================================================

FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source (will be overridden by volume mount in docker-compose)
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server
# --host 0.0.0.0 allows access from outside container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]